package com.mo.economy_system.common.market;

/** Translation keys shared by both target notification adapters. */
public final class MarketExpirationFeedback {
  public static final String SALES_RETURN = "message.market.expired.sales_return";
  public static final String DEMAND_REFUNDED = "message.market.expired.demand_refunded";
  public static final String DEMAND_DELIVERED = "message.market.expired.demand_delivered";

  private MarketExpirationFeedback() {}
}
