package com.mo.economy_system.common.network;

import com.mo.economy_system.platform.network.EconomyNetworkMessage;

/** Requests the current server-owned system-shop catalog. */
public enum ShopDataRequestMessage implements EconomyNetworkMessage {
    INSTANCE
}
