package com.mo.economy_system.ui.market;

/** Semantic actions exposed by either market order form. */
public enum MarketCreateAction {
  SELECT_SLOT,
  DECREMENT,
  INCREMENT,
  SELECT_ALL,
  SUBMIT,
  BACK
}
