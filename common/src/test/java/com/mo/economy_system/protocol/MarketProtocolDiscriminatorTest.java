package com.mo.economy_system.protocol;import org.junit.jupiter.api.Test;import static org.junit.jupiter.api.Assertions.*;class MarketProtocolDiscriminatorTest{@Test void canonicalOrderProtocolNumbersNeverMove(){assertEquals(12,EconomyProtocol.PURCHASE_SALES_ORDER.discriminator());assertEquals(13,EconomyProtocol.CONFIRM_DEMAND_ORDER.discriminator());assertEquals(14,EconomyProtocol.DELIVER_DEMAND_ORDER.discriminator());assertEquals(15,EconomyProtocol.REMOVE_SALES_ORDER.discriminator());assertEquals(16,EconomyProtocol.REMOVE_DEMAND_ORDER.discriminator());}}
